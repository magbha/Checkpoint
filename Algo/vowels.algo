ALGORITHM vowels

VAR
    str : STRING := "THis is AwESowme"
    vowels : CHAR := ['a', 'e', 'i', 'o','u']
    count : INTEGER := 0
BEGIN
    FOR i in str DO
     IF i in vowels THEN
     count := 1
     END_IF
    END_FOR
    Write(count)
END